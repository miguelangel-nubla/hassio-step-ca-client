---
name: Lock

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch:

jobs:
  workflows:
    uses: miguelangel-nubla/hassio-workflows/.github/workflows/lock.yaml@main
